# Sanity CMS — configurar quando o projeto Sanity for criado.
# Sem essas variáveis, o site usa dados mock locais.
# Copie este arquivo para .env (não comitar) e preencha com os valores reais.

PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production
